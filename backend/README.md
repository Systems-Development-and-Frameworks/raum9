# News backend

## Build Setup

``` bash
npm install
```

# Start server

Create a `.env` file in your working directory.

```
JWT_SECRET=<ypur secret>
```

To run the server:
```bash
npm run server
```

# Run linter

```bash
npm run lint
```
